@page
@model asp_presentacion.Pages.Ventanas.ReservasModel
@{
    ViewData["Titulo"] = "";
}
<center>
    <div>
        <form method="POST"
              enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div align="left">
                <h3>Información de reservas</h3>
            </div>
            <div align="left"
                 class="bt_panel_buttons">
                <input type="submit"
                       value="Refrescar"
                       asp-page-handler="BtRefrescar"
                       class="btn btn-secondary"
                       disabled="@(Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <input type="submit"
                       value="Nuevo"
                       asp-page-handler="BtNuevo"
                       class="btn btn-secondary"
                       disabled="@(Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <input type="submit"
                       value="Guardar"
                       asp-page-handler="BtGuardar"
                       class="btn btn-secondary"
                       disabled="@(Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <input type="submit"
                       value="Cancelar"
                       asp-page-handler="BtCancelar"
                       class="btn btn-secondary"
                       disabled="@(Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <a asp-page="/Index" class="btn ghi-btn-home ms-2">Regresar al Inicio</a>
            </div>

            @if (Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas ||
                        Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <input type="hidden"
                       asp-for="@Model.Actual!.Id" />
                <input type="hidden"
                       asp-for="@Model.Actual!.CheckIn" />
                <input type="hidden"
                       asp-for="@Model.Actual!.CheckOut" />
                <input type="hidden"
                       asp-for="@Model.Actual!.IdCliente" />
            }
            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <input type="hidden"
                       asp-for="@Model.Actual!.Id" />
                <input type="hidden"
                       asp-for="@Model.Actual!.CheckIn" />
                <input type="hidden"
                       asp-for="@Model.Actual!.CheckOut" />
                <input type="hidden"
                       asp-for="@Model.Actual!.IdCliente" />
            }

            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Editar)
            {
                <div class="containers_vn panel panel-default">
                    <h3>Reservas registradas</h3>

                    <div>
                        <div class="form-group">
                            <label class="col-lg-4 control-label">
                                ID
                            </label>
                            <div class="col-lg-6">
                                <input id="nmId"
                                       class="cts_forms form-control"
                                       asp-for="@Model.Actual!.Id" />
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-lg-4 control-label">
                                Fecha de entrada
                            </label>
                            <div class="col-lg-6">
                                <input id="nmCheckIn"
                                       class="cts_forms form-control"
                                       asp-for="@Model.Actual!.CheckIn" />
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-lg-4 control-label">
                                Fecha de salida
                            </label>
                            <div class="col-lg-6">
                                <input id="nmCheckOut"
                                       class="cts_forms form-control"
                                       asp-for="@Model.Actual!.CheckOut" />
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-lg-4 control-label">
                                ID del cliente
                            </label>
                            <div class="col-lg-6">
                                <input id="nmIdCliente"
                                       class="cts_forms form-control"
                                       asp-for="@Model.Actual!.IdCliente" />
                            </div>
                        </div>
                    </div>
                </div>
            }

            @if (ViewData.ContainsKey("Mensaje"))
            {
                <input type="hidden"
                       asp-for="@Model.Accion" />
                @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
                ;
            }

            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <div class='popup'>
                    <div class='pp_del_inner'>
                        <div style='width:95%'>
                            <h2 align="left">
                                Mensaje
                            </h2>

                            <h4 align="left">
                                ¿Desea borrar esta reserva?
                            </h4>
                            <h6 align="left">
                                @Model.Actual!.Id,
                                @Model.Actual!.CheckIn,
                                @Model.Actual!.CheckOut
                            </h6>
                            <br />
                            <br />
                            <input type="submit"
                                   value="Borrar"
                                   asp-page-handler="BtBorrar"
                                   class="btn btn-secondary" />
                            <input type="submit"
                                   value="Cerrar"
                                   asp-page-handler="BtCancelar"
                                   class="btn btn-secondary" />
                        </div>
                    </div>
                </div>
            }

            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas ||
                        Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                /*<input id="flIdTelefonoAcompañante"
                       class="cts_forms form-control"
                       asp-for="@Model.Filtro!.IdTelefonoAcompañante"
                       placeholder="IdTelefonoAcompañante" />
                <br />*/

                <div class="containers_vn panel panel-default">


                    <table class="tb_data table table-striped table-bordered table
    hover">
                        <tr>
                            <th>
                                Id
                            </th>
                            <th>
                                Fecha de entrada
                            </th>
                            <th>
                                Fecha de salida
                            </th>
                            <th class="cl_hidden">
                                IdCliente
                            </th>
                            <th class="cl_actions">
                                Accion
                            </th>
                        </tr>
                        @if (Model != null && Model.Lista != null)
                        {
                            @foreach (var elemento in Model.Lista)
                            {
                                <tr>
                                    <td>
                                        @elemento.Id
                                    </td>
                                    <td>
                                        @elemento.CheckIn
                                    </td>
                                    <td class="cl_hidden">
                                        @elemento.CheckOut
                                    </td>
                                    <td class="cl_hidden">
                                        @elemento.IdCliente
                                    </td>
                                    <td>
                                        <input type="submit"
                                               value="Modicar"
                                               asp-page-handler="BtModificar"
                                               class="btn btn-secondary"
                                               asp-route-data="@elemento.Id" />
                                        <input type="submit"
                                               value="Borrar"
                                               asp-page-handler="BtBorrarVal"
                                               class="btn btn-secondary"
                                               asp-route-data="@elemento.Id" />
                                    </td>
                                </tr>
                            }
                        }
                    </table>
                </div>
            }
        </form>
    </div>
</center>